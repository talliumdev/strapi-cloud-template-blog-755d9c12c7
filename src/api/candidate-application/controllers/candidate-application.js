'use strict';

/**
 * candidate-application controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::candidate-application.candidate-application');
